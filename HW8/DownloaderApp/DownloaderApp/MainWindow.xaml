<Window x:Class="DownloaderApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloaderApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="10">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="URL:" VerticalAlignment="Center" Width="40"/>
                <TextBox x:Name="UrlTextBox" Width="500" Margin="5,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">

                <Button Content="Choose file"
                Width="100"
                Margin="5"
                Click="ChooseFile_Click"/>

                <Button Content="Download"
                Width="100"
                Margin="5"
                Background="LightGreen"
                Click="Download_Click"/>

                <Button Content="Cancel"
                Width="100"
                Margin="5"
                Background="LightCoral"
                Click="Cancel_Click"/>

            </StackPanel>

            <DataGrid Grid.Row="2"
              x:Name="DownloadsGrid"
              AutoGenerateColumns="False"
              IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="File"
                                Binding="{Binding FileName}"
                                Width="*"/>

                    <DataGridTextColumn Header="Size"
                                Binding="{Binding TotalBytes}"
                                Width="100"/>

                    <DataGridTextColumn Header="Status"
                                Binding="{Binding Status}"
                                Width="150"/>

                </DataGrid.Columns>

            </DataGrid>

        </Grid>
    </Grid>
</Window>
